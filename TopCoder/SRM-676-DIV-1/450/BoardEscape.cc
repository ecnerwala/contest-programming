#include <bits/stdc++.h>

typedef long long ll;

typedef pair<int, int> pii;
#define A first
#define B second

class BoardEscape {

public:

	string findWinner(vector <string> s, int k) {
	}
};

// vim:ft=cpp
